export default {
  name: "Personal",
  data() {
    return {
      // userInfo: {},
      // statistics: {}
    };
  },
  mounted() {
    // 获取用户信息
    // this.userInfo = JSON.parse(window.localStorage.getItem("userInfo"));
    // this.getStatisticsData();
  },
  methods: {
    // getStatisticsData() {
    //   this.$api.platform.getStatistics().then(res => {
    //     this.statistics = res.statistics;
    //   });
    // }
  }
};
